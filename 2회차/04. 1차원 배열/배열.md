# 배열

```
- 1차원 배열 선언 및 크기 할당
자료형[] a = new 자료형[4];

- 2차원 배열 선언 및 크기 할당
자료형[][] a = new 자료형[3][4];

- 배열 오름차순 정렬
import java.util.*;
Arrays.sort(a);로 사용

- 배열 내림차순 정렬
import java.util.*;
Arrays.sort(a, Comparator.reverseOrder());
```